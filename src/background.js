// // background.js

// chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
//     if (message.type === 'closeExtension') {
//       // Add the logic to close the extension
//       // For example, you can use chrome.runtime.reload();
//       // or chrome.runtime.sendMessage({ type: 'closePopup' });
//     }
//   });